<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net6.0</TargetFramework>
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
        <PackageVersion />
        <Description>A dummy repo for testing CI/CD pipelines and build tools.</Description>
        <RepositoryUrl>https://github.com/mc738/TestRepo</RepositoryUrl>
        <PackageProjectUrl>https://github.com/mc738/TestRepo</PackageProjectUrl>
    </PropertyGroup>

    <ItemGroup>
        <Compile Include="Library.fs" />
    </ItemGroup>

</Project>
